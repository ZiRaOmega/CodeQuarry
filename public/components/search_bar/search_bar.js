search_bar_input = document.getElementById('search-bar-input');

// Two steps. First, detect each keypress in the search bar input field and do something with it.
// Second, when the user presses Enter, submit the search query to the server.

// Step 1: Detect each keypress in the search bar input field and do something with it.
// Don't forget to handle the Backspace key, which is key code 8.
search_bar_input.addEventListener('keypress', function(e) {
});
